{
  "name": "hkf-cl-world-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env src/server.js",
    "deploy": "tcb fn deploy hkf-cl-world-api --httpFn --dir .",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "@cloudbase/node-sdk": "^3.16.0"
  },
  "engines": {
    "node": ">=20"
  }
}
