# FL小世界 CloudBase 部署和使用教程

这份教程按第一次接触部署的用户来写。完成后，手机上的 App 会访问腾讯云国内后端，不需要开启 VPN。旧的 Cloudflare 数据不会迁移，新的后端从空数据开始。

## 1. 你将部署什么

这个 App 由两部分组成：

- 手机 App：项目里的 `android/` 文件夹，用 Android Studio 安装到手机。
- 后端服务：项目里的 `server/` 文件夹，部署到腾讯云 CloudBase。

后端会使用这些服务：

- **HTTP 云函数**：接收手机请求。
- **云数据库和云存储**：保存聊天记录、留言、日历、位置和相册文件。
- **DeepSeek API**：使用你自己的 API Key 和 `deepseek-v4-flash` 生成聊天回复。

## 2. 电脑准备

安装以下软件：

1. Node.js 20 或更高版本：<https://nodejs.org/>
2. Android Studio：<https://developer.android.google.cn/studio>
3. JDK 17。Android Studio 通常自带合适的 JDK。

打开终端，在项目根目录确认 Node.js 可用：

```bash
node --version
npm --version
```

安装 CloudBase 命令行工具并登录：

```bash
npm install -g @cloudbase/cli
tcb login
```

命令会打开浏览器，请使用你的腾讯云账号完成登录。

## 3. 创建 CloudBase 环境

1. 打开腾讯云 CloudBase 控制台：<https://tcb.cloud.tencent.com/>
2. 点击创建环境。
3. 地域选择中国大陆地域，例如**上海**。
4. 环境名称可填写 `flworld`。
5. 创建完成后，记录页面显示的**环境 ID**，例如 `flworld-1gxxxxxx`。

后面看到“你的环境 ID”时，都填这一串真实内容。

## 4. 创建数据库集合

你在控制台看到的 **SQL 型数据库** 和 **文档型数据库** 是两种不同的数据库。本 App 后端代码使用的是文档集合，因此这里一定选择：

```text
文档型数据库
```

不要选择 SQL 型数据库，也不需要新建 SQL 表。

### 第一次打开文档型数据库

1. 在 CloudBase 环境概览页左侧菜单找到**数据库**，点击进入。
2. 点击页面中的**文档型数据库**卡片或页签。
3. 如果页面提示需要开通或初始化文档型数据库，按提示点击**开通**或**立即使用**。
4. 开通完成后，你会进入文档型数据库页面。页面左侧应有一个**集合列表**区域。

### 创建一个集合的方法

在文档型数据库页面：

1. 找到左侧**集合列表**标题附近的 `+` 按钮，点击它。
2. 在“新建集合”窗口中输入集合名称，例如先输入：

```text
features
```

3. 如果窗口要求选择权限，选择最严格的服务端访问选项，常见显示名称为：

```text
仅管理员可读写
```

或：

```text
所有用户不可读写，仅云函数可读写
```

本 App 的手机端不会直接连接数据库，所有读写都由 HTTP 云函数完成，所以不要选择“所有用户可读”。

4. 点击**确定**或**创建**。
5. 创建完成后，左侧列表会出现刚刚创建的集合名称。

### 需要创建的全部集合

重复上面的创建操作，逐个新建下面 12 个集合。集合名称必须完全一致，全部使用英文小写和下划线：

```text
features
personas
sessions
messages
notes
calendar_events
album_items
locations
auth_sessions
memory_documents
ai_memories
chat_requests
```

创建完毕后，左侧集合列表应能看到这 12 项：

| 集合名称 | 保存内容 |
| --- | --- |
| `features` | 首页功能卡片 |
| `personas` | 聊天风格 |
| `sessions` | 聊天会话 |
| `messages` | 聊天消息 |
| `notes` | 留言 |
| `calendar_events` | 日历事项 |
| `album_items` | 相册文件信息 |
| `locations` | 两位用户的粗略位置 |
| `auth_sessions` | 每个身份当前有效的设备登录会话 |
| `memory_documents` | 双方共同编辑的回忆文档 |
| `ai_memories` | 小暖提炼并可由双方修改的记忆卡片 |
| `chat_requests` | 聊天消息幂等去重记录 |

不需要点击“添加文档”，也不需要手工填写 JSON 数据。第一次成功请求 App 后，后端会自动向 `features` 和 `personas` 写入默认数据，其余集合会在使用对应功能后出现数据。

## 5. 准备云存储和 DeepSeek API Key

### 云存储

1. 在同一环境中打开**云存储**。
2. 按控制台提示开通默认存储空间。
3. 不需要预先创建文件夹；第一次上传照片时后端会自动使用 `album/` 路径。

### DeepSeek API Key

1. 打开 DeepSeek 开放平台：<https://platform.deepseek.com/>
2. 登录后进入 API Keys 页面。
3. 如果你之前部署旧后端时已有可用 API Key，可以继续使用原来的 key；否则新建一个。
4. 妥善保存这个 API Key，后面会把它写入云函数环境变量。

如果 API Key 无效或账户余额不足，其余共享功能仍可工作，聊天会显示后端内置的提示文字。

## 6. 设置后端登录口令

App 固定有两个身份：

| App 显示名 | 内部账号 | 用途 |
| --- | --- | --- |
| 锋宝 | `hkf` | 第一位用户 |
| 璐宝 | `cl` | 第二位用户 |

请准备两段仅你们知道的文本：

- `LOGIN_CODE_HKF`：锋宝登录时输入的口令。
- `LOGIN_CODE_CL`：璐宝登录时输入的口令。
登录成功后由后端为当前设备自动签发 token。同一身份在新设备登录时，旧设备会退出以避免身份和位置混淆。

## 7. 安装后端依赖并检查

在终端进入本项目的 `server` 目录：

```bash
cd server
npm install
npm run check
```

`npm run check` 没有输出报错就可以继续。

## 8. 部署 HTTP 云函数

仍在 `server` 目录执行：

```bash
tcb fn deploy hkf-cl-world-api --httpFn --dir .
```

部署时：

1. 环境请选择第 3 步创建的环境。
2. 如果询问是否自动生成启动脚本，选择否；项目已经包含 `scf_bootstrap`。
3. 如果提示覆盖同名函数，第一次部署不会出现；以后更新时确认覆盖即可。

部署完成后进入 CloudBase 控制台，找到 `hkf-cl-world-api` 函数并进行配置：

1. **超时时间**设置为 `120` 秒，以便 AI 流式回复有足够时间完成。
2. **内存**建议选择 `512 MB`。
3. 在**环境变量**处填入：

```text
LOGIN_CODE_HKF=锋宝口令
LOGIN_CODE_CL=璐宝口令
LLM_API_KEY=你的DeepSeek_API_Key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-flash
LLM_TIMEOUT_MS=60000
```

4. 打开函数的**网络配置**，确认已开启**公网访问**。DeepSeek API 是公网服务，函数必须能够从运行环境主动访问 `https://api.deepseek.com`。
5. 如果函数已接入私有网络（VPC），不要只保留“仅内网访问”：改为同时开启内网和公网访问，或为该 VPC 配置 NAT 出网。

保存后，按控制台提示重新部署或发布函数配置。注意：下一节配置的是“手机访问云函数”的入站域名，与“云函数访问 DeepSeek”的出站公网能力是两项独立配置。

## 9. 给函数配置可访问的网址

1. 在 CloudBase 控制台进入 **HTTP 访问服务**。
2. 新建域名关联资源。
3. 资源类型选择**云函数**，函数选择 `hkf-cl-world-api`。
4. 域名选择控制台提供的**默认域名**。
5. 触发路径填写 `/`，这样 App 的接口地址无需增加额外路径。
6. 保存配置。

页面会显示一个以 `https://` 开头的网址，例如：

```text
https://你的环境默认域名
```

在浏览器中打开下面地址测试，将域名替换成你的真实地址：

```text
https://你的环境默认域名/health
```

看到以下内容表示后端入口已经上线：

```json
{"ok":true}
```

## 10. 把后端地址写入 Android App

在项目的 `android/gradle.properties` 文件末尾新增一行，把地址换成第 9 步复制的真实域名，末尾不要加 `/`：

```properties
FLWORLD_API_BASE_URL=https://你的环境默认域名
```

例如你的地址是 `https://abc.ap-shanghai.app.tcloudbase.com`，就填写：

```properties
FLWORLD_API_BASE_URL=https://abc.ap-shanghai.app.tcloudbase.com
```

## 11. 安装并使用 App

1. 打开 Android Studio。
2. 选择打开项目中的 `android/` 目录。
3. 等待右下角 Gradle 同步完成。
4. 用数据线连接安卓手机并在手机上允许 USB 调试，或者启动模拟器。
5. 点击 Android Studio 顶部运行按钮安装 App。

登录时：

- 选择“锋宝”时输入 `LOGIN_CODE_HKF` 设置的口令。
- 选择“璐宝”时输入 `LOGIN_CODE_CL` 设置的口令。

首次登录后可以按顺序测试：

1. 打开聊天，发送一句话，看是否逐字出现回复。
2. 进入“我想对你说”，写一条留言。
3. 添加一个日历事项。
4. 两台手机都允许粗略定位后查看距离。
5. 在相册上传一张图片，并在另一台手机查看。
6. 进入“回忆”写一篇共同文档，点击“让小暖翻翻我们的故事”验证记忆卡片。

相册总容量限制为 `200 MB`。相册文件保存在 CloudBase 云存储中，数据库仅记录名字、大小和预览信息。

## 12. 以后修改代码后的重新部署

### 只修改后端

```bash
cd server
npm run check
tcb fn deploy hkf-cl-world-api --httpFn --dir .
```

不需要重新安装手机 App。

### 只修改 Android 界面

不需要部署后端，直接在 Android Studio 重新运行 App。

### CloudBase 默认域名变化

一般不会变化。如果你换了 CloudBase 环境或改用自定义域名，重新修改：

```properties
FLWORLD_API_BASE_URL=https://新的域名
```

然后在 Android Studio 重新运行 App。

## 13. 常见问题

### 浏览器打不开 `/health`

- 确认 CloudBase 环境地域是中国大陆地域。
- 确认 HTTP 访问服务已经把默认域名绑定到 `hkf-cl-world-api`。
- 确认触发路径是 `/`。
- 到云函数日志页面查看启动报错；启动脚本名称必须是 `scf_bootstrap`。

### App 一直提示请求失败

- 先确认手机浏览器能够直接打开 `https://你的域名/health`。
- 检查 `android/gradle.properties` 中 `FLWORLD_API_BASE_URL` 是否是真实地址、没有引号、没有末尾 `/`。
- 修改地址后必须重新安装或重新运行 App。

### 登录失败

- 确认云函数环境变量里的 `LOGIN_CODE_HKF` 和 `LOGIN_CODE_CL` 已保存并发布。
- 注意选择的身份必须匹配对应口令。
- 修改环境变量后建议重新部署函数再测试。

### 聊天只有固定提示文字

- 确认云函数环境变量 `LLM_API_KEY` 已填写可用的 DeepSeek API Key。
- 确认 `LLM_BASE_URL=https://api.deepseek.com` 与 `LLM_MODEL=deepseek-v4-flash`。
- 确认函数网络配置开启了**公网访问**；如果函数使用 VPC，则还需要同时允许公网访问或配置 NAT 出网。
- 在 DeepSeek 平台确认 API Key 可用且账户有可用额度。
- 检查云函数日志：`LLM HTTP failure` 表示已连通 DeepSeek，再检查 key、额度和请求参数；`LLM network failure` 且包含 `software caused connection abort`、`ECONNRESET` 或超时，优先检查公网出站配置与安全策略。

### 上传照片或视频失败

- 确认云存储已开通。
- 确认所有相册文件总大小没有超过 `200 MB`。
- 当前 App 会通过后端上传整个文件，建议优先上传照片和较短的视频，避免上传超大的视频文件。

### 如何查看运行错误

进入 CloudBase 控制台，选择该环境中的云函数 `hkf-cl-world-api`，打开**日志**。手机提示失败时，再回到日志页面查看对应时间附近的错误内容。

## 14. 官方参考文档

- CloudBase HTTP 云函数部署：<https://docs.cloudbase.net/cli-v1/functions/deploy>
- SSE 流式响应：<https://docs.cloudbase.net/cloud-function/develop/sse>
- DeepSeek API 文档：<https://api-docs.deepseek.com/>
- CloudBase 云存储 Node SDK：<https://docs.cloudbase.net/api-reference/server/node-sdk/storage>
