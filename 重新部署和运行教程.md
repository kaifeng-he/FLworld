# FL小世界重新部署和运行教程

这份教程用于代码改完之后，重新部署后端、迁移数据库，并在安卓手机上重新运行 App。

## 1. 准备环境

电脑需要先装好：

- Node.js 20 或更新版本
- Android Studio
- JDK 17
- Android SDK 35

第一次使用 Cloudflare 部署时，还需要登录一次：

```bash
cd server
npx wrangler login
```

如果已经登录过，可以跳过这一步。

## 2. 安装后端依赖

在项目根目录执行：

```bash
cd server
npm install
```

后续如果 `package.json` 没有变化，不一定每次都要重新执行 `npm install`。

## 3. 检查后端代码

部署前先做一次语法检查：

```bash
cd server
npm run check
```

如果这里报错，先修复后再继续部署。

## 4. 迁移远程数据库

Version 3 增加了聊天风格颜色字段，所以重新部署前需要把远程 D1 数据库迁移到最新结构：

```bash
cd server
npm run d1:migrate
```

这个命令会执行 `server/migrations/` 里的迁移文件，包括：

- `0001_init.sql`
- `0002_version2.sql`
- `0003_version3.sql`

正常情况下，已经执行过的迁移不会重复破坏已有数据。

## 5. 确认大模型密钥

如果之前没有设置过大模型密钥，需要执行：

```bash
cd server
npx wrangler secret put LLM_API_KEY
```

然后按提示粘贴 DeepSeek API Key。

如果之前已经设置过，通常不需要重复设置。

当前默认模型配置在 `server/wrangler.toml`：

```toml
LLM_BASE_URL = "https://api.deepseek.com"
LLM_MODEL = "deepseek-v4-flash"
```

## 6. 部署后端

执行：

```bash
cd server
npm run deploy
```

部署完成后，Cloudflare Worker 地址应为：

```text
https://hkf-cl-world.flworld.workers.dev
```

Android App 已经把这个地址写在代码里，不需要在 App 里填写后端地址。

## 7. 验证后端是否可用

部署后可以用浏览器打开：

```text
https://hkf-cl-world.flworld.workers.dev/health
```

如果看到类似下面的内容，说明后端已经在线：

```json
{"ok":true}
```

## 8. 重新运行 Android App

1. 打开 Android Studio。
2. 选择打开项目里的 `android/` 目录。
3. 等待 Gradle 同步完成。
4. 用数据线连接安卓手机，或启动模拟器。
5. 点击 Android Studio 顶部的运行按钮。

App 名称应显示为：

```text
FL小世界
```

登录时选择身份：

- 锋宝：内部账号是 `hkf`
- 璐宝：内部账号是 `cl`

默认口令在 `server/wrangler.toml` 中：

```toml
LOGIN_CODE_HKF = "hkf"
LOGIN_CODE_CL = "cl"
```

如果你改成了更私密的口令，就用修改后的口令登录。

## 9. 每次更新后的推荐流程

以后每次改完代码，按这个顺序来：

```bash
cd server
npm run check
npm run d1:migrate
npm run deploy
```

然后在 Android Studio 里重新运行 App。

如果只改了安卓界面，没有改后端，可以不执行后端部署，只需要在 Android Studio 里重新运行 App。

如果只改了后端代码，没有改安卓代码，执行：

```bash
cd server
npm run check
npm run deploy
```

如果新增或修改了数据库迁移文件，也要执行：

```bash
cd server
npm run d1:migrate
```

## 10. 常见问题

### App 打不开或请求失败

先确认后端健康检查能打开：

```text
https://hkf-cl-world.flworld.workers.dev/health
```

如果打不开，说明后端部署或 Cloudflare 配置有问题。

### 登录失败

检查 `server/wrangler.toml` 里的登录口令，或确认线上是否设置了对应 secret。

默认本地配置是：

```toml
LOGIN_CODE_HKF = "hkf"
LOGIN_CODE_CL = "cl"
```

### 聊天机器人不回复或回复兜底内容

确认已经设置：

```bash
npx wrangler secret put LLM_API_KEY
```

如果没有设置，大模型调用会失败或使用兜底回复。

### 相册上传失败

Version 2 相册总容量限制是 200兆。超过限制后，需要先删除一些旧照片或视频。

相册媒体数据存储在 D1 中，适合少量精选照片和短视频，不适合长期大量视频存储。

### Android Studio 找不到 Gradle 命令

这个项目当前没有提交 Gradle Wrapper。推荐直接用 Android Studio 打开 `android/` 目录运行。

如果需要命令行构建，可以在 Android Studio 或本机环境中配置 Gradle 后执行：

```bash
cd android
gradle :app:assembleDebug
```
